# No version floors on purpose -- see fantasy-football-db/scripts/requirements.txt
fastapi
uvicorn[standard]
jinja2
duckdb
python-multipart  # needed for Form(...) request bodies, e.g. the /games/pickem picks form
