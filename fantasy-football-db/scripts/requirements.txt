# No version floors on purpose -- an overly strict pin (e.g. duckdb>=1.5.0)
# can fail to resolve at all on an older Python/pip, aborting the entire
# install even though a slightly older version would work fine.
duckdb
requests
pandas
numpy
statsmodels
scikit-learn
openpyxl        # load_pokemon_historical_seasons.py reads the historical .xlsx league docs
